# Database konfiguration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=sagshub
DB_USER=postgres
DB_PASSWORD=wa2657321

# Server konfiguration
PORT=3000
HOST=0.0.0.0

# Session secret (skift til noget sikkert i produktion)
SESSION_SECRET=your-super-secret-session-key-here

# Netværk konfiguration
# Sæt til din computers IP-adresse for at tillade eksterne forbindelser
# Eksempel: EXTERNAL_HOST=192.168.1.100
EXTERNAL_HOST=localhost

# Frontend URL (bruges til CORS)
FRONTEND_URL=http://localhost:5174

# Miljø
NODE_ENV=production 